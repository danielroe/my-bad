# nitro-v3 fixture

Minimal Nitro 3 app with a `my-bad` error handler, exercised by `test/nitro-v3.test.ts`.

`nitro.config.ts` aliases `my-bad` to `dist/`, so run `pnpm build` at the repo root before running the test.
